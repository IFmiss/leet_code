function getRow(rowIndex: number): number[] {
  if (rowIndex = 1) return [1]
  if (rowIndex == 2) return [1, 1]
  let arr = []
  // arr.push()
  // for(let i = 0; i < rowIndex; i ++) {
  //   arr.push(getRow(i)[ro])
  // }
};